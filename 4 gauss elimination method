import numpy as np

A = np.array([[3, 6, 1], 
              [2, 4, 3], 
              [1, 3, 2]], dtype=float)

b = np.array([16, 13, 9], dtype=float)

solution = np.linalg.solve(A, b)

print("Solution (Using NumPy):", solution)
